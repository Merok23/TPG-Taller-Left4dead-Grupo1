enum State {
    IDLE,
    RUN,
    SHOOT,
    RELOAD, 
    DIE
};

enum EntityType {
    SOLDIER,
    COMMON_INFECTED,
};

enum WeaponType {
    IDF,
    P90,
    SCOUT,
    NONE
};

enum GameMode {
    SURVIVAL,
    CLEAR_THE_ZONE
};