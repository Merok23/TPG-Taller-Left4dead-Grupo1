enum GameMode {
    SURVIVAL,
    CLEAR_THE_ZONE,
    TESTING
};

enum Cheat {
    INFINITE_HITPOINTS,
    SPAWN_COMMON_INFECTED,
    KILL_ALL_INFECTED
};

enum InfectedType {
    COMMON,
    SPEAR,
    WITCH
};